name: Github Pipeline

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
        default: 'dev'
      project-name:
        required: true
        type: string
      project-type:
        required: true
        type: string
        
      doBuild:
        type: boolean
        default: true
        
      # .NET-based apps
      dotnet-version:
        type: string
        default: '5.0.x'
        
      # Node-based apps
      node-version:
        type: string
        default: '14.x'

jobs:
  build:
    if: ${{ inputs.doBuild }}
    runs-on: ubuntu-latest
    
    steps:
      - name: 'Build ${{ inputs.project-type }}'
#         if: ${{ inputs.doBuild && inputs.project-type == 'Angular' }}
        uses: soo-doh-kohd/pipeline/.github/actions/build-${{ github.event.inputs.project-type }}@main
        with:
          environment: ${{ inputs.environment }}
          project-name: ${{ inputs.project-name }}
          node-version: ${{ inputs.node-version }}

#       - name: 'Build .NET Core'
#         if: ${{ inputs.doBuild && inputs.project-type == '.NET Core' }}
#         uses: soo-doh-kohd/pipeline/.github/actions/build-dotnet-core@main
#         with:
#           environment: ${{ inputs.environment }}
#           project-name: ${{ inputs.project-name }}
#           dotnet-version: ${{ inputs.dotnet-version }}

